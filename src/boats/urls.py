from django.urls import pathfrom .views import (BoatListView,                    boat_list_view,                    BoatDetailView,                    boat_detail_view,                    BoatActiveListView,                    BoatActiveDetailView,                    BoatDetailSlugView)urlpatterns = [    path('', BoatListView.as_view()),    path('<slug:slug>/', BoatDetailSlugView.as_view()),]
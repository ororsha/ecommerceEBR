from django.urls import pathfrom .views import (BoatListView,                    BoatDetailSlugView)urlpatterns = [    path('', BoatListView.as_view(), name='list'),    path('<slug:slug>/', BoatDetailSlugView.as_view(), name='detail'),]